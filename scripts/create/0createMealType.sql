CREATE TABLE MEAL_TYPE(
    meal_type_id           VARCHAR(4)    NOT NULL,
    meal_type_name         VARCHAR(20)   NOT NULL,
    meal_type_description  VARCHAR(100),
    PRIMARY KEY (meal_type_id)
);